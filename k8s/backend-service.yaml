apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: enterprise-pm
spec:
  type: ClusterIP
  selector:
    app: pm-backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
